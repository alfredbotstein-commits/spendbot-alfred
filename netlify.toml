[build]
  publish = "dist"
  command = "npm run build"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA catch-all - Netlify automatically handles /.netlify/functions routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
